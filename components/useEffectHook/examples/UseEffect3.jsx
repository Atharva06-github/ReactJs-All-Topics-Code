import React from 'react'

const UseEffect3 = () => {

    //if we pass any dependency into dependency array,useEffects callback function will get called only when there is some change's into the dependency.This useEffects callback function will get called everytime whenever there is change into the dependency value
  return (
    <div>
      
    </div>
  )
}

export default UseEffect3
